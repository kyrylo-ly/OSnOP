# 1) Програмний код інтерпретатора (п.5-6)

Файл програмного коду:

- `lab12/extarithexpr.py`

Що реалізовано відповідно до п.5-6:

- Бінарні операції: `+`, `-`, `*`, `/`, `//`, `%`.
- Піднесення до степеня: `**` (правоасоціативне, вищий пріоритет за `*`/`/`/`//`/`%`).
- Функції: `sin(x)` і `cos(x)`.
- Унарні операції: `+` і `-`.
- Числа: цілі, фіксована крапка, експоненційний запис (`45.02`, `1.0e-5`, `28e4`).

Коментарі в коді:

- У файлі `lab12/extarithexpr.py` додані змістовні коментарі до кожного доопрацювання (сканер, граматика, пріоритети, помилки, тести).
